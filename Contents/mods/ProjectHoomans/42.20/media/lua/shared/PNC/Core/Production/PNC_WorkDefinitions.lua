-- Compatibility entry point. The implementation lives in WorkDefinition so
-- callers that still use the original public require path keep working.
return require "PNC/Core/Production/WorkDefinition/PNC_WorkDefinitions"
