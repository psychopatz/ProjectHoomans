-- Compatibility path for callers that still require the legacy module name.
-- Territory authoring now belongs to the Command Hub Base branch.
return require "PNC/UI/CommandHub/PNC_CommandHub_BaseTerritoryActions"
